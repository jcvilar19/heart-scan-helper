# Cardiomegaly Model

This folder is reserved for the Python model code.

- `predict.py` is intentionally empty — implement your inference logic there.
- The current web UI uses a **mocked** classifier in
  `src/lib/classifier.ts`. Replace `mockClassify` with a `fetch()` call to your
  Python service (FastAPI / Flask) once it is deployed.
